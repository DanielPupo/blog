# Produção anônima: gere com python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=troque-por-um-segredo-aleatorio
ANONYMOUS_MODE=true
DATABASE_ENABLED=false

# Preencha somente quando autenticação e MySQL forem habilitados.
ADMIN_USERNAME=admin
# Gere com python -c "from werkzeug.security import generate_password_hash; print(generate_password_hash('sua-senha'))"
ADMIN_PASSWORD_HASH=
DB_HOST=
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=blog_pupo

ENABLE_LOCAL_UPLOADS=false
FLASK_DEBUG=false
