<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url><loc>{{ url_for('index', _external=True) }}</loc></url>
  {% for post in posts %}
  <url>
    <loc>{{ url_for('post_detail', post_id=post.idPost, _external=True) }}</loc>
    <lastmod>{{ post.datePost.strftime('%Y-%m-%d') }}</lastmod>
  </url>
  {% endfor %}
</urlset>
